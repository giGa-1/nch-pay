import GenLink from '@/components/GenLink/GenLink'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <main>
    <GenLink/>
  </main>
}

export default page