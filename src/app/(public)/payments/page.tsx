
import PaymentPage from '@/components/Payments/PaymentPage'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <main>
    <PaymentPage/>
  </main>
}

export default page